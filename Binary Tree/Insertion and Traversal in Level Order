🟩 Problem

You are organizing a cricket tournament using a binary tree structure.
Teams are inserted level by level based on their registration order.
After inserting all teams, you must display the tournament bracket using level-order traversal (top to bottom, left to right).
---

🟩 Input

* Integer N — number of teams
* N integers — team rankings inserted in order
---
🟩 Output - Print in level-order traversal
---

🟩 Sample Input
```
6
8 3 10 1 6 14
```

🟩 *Sample Output
```
8 3 10 1 6 14
```

🟩 Java Solution (Insertion + Traversal - both in Level-Order )

import java.util.*;

public class Main {

    static class Node {
        int data;
        Node left, right;

        Node(int data) {
            this.data = data;
        }
    }

    static Node root;

    // Insert nodes in level order
    static void insert(int[] arr) {

        if (arr.length == 0) return;

        root = new Node(arr[0]);
        Queue<Node> q = new LinkedList<>();
        q.add(root);

        int i = 1;

        while (i < arr.length) {
            Node curr = q.poll();

            if (i < arr.length) {
                curr.left = new Node(arr[i]);
                q.add(curr.left);
                i++;
            }

            if (i < arr.length) {
                curr.right = new Node(arr[i]);
                q.add(curr.right);
                i++;
            }
        }
    }

    // Level-order traversal
    static void levelOrder() {

        Queue<Node> q = new LinkedList<>();
        q.add(root);

        while (!q.isEmpty()) {
            Node curr = q.poll();
            System.out.print(curr.data + " ");

            if (curr.left != null)
                q.add(curr.left);

            if (curr.right != null)
                q.add(curr.right);
        }
    }

    public static void main(String[] args) {

        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        int[] arr = new int[n];

        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        insert(arr);
        levelOrder();
    }
}
```

---

🟩 Explanation

* Teams are inserted level by level using a queue datastructure as helper
* Level-order traversal prints nodes in the same top-to-bottom order.
* No recursion needed.

---

🟩Time Complexity - O(n) — each node is inserted and visited once
🟩 Space Complexity - O(n) — queue stores tree nodes during insertion and traversal
