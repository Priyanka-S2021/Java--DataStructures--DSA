🟩 Problem
You have a linked list of task IDs representing ongoing tasks.
You need to delete the last M tasks from the list.
After deletion, print the remaining tasks separated by space.
If no tasks remain, print "List is empty".

---
🟩 Sample Input
```
6
201 202 203 204 205 206
4
```
---
🟩 Sample Output
```
201 202
```
---
🟩 Solution

import java.util.*;

public class Solution {
    class Node {
        int data;
        Node next;
        Node(int data) {
            this.data = data;
            this.next = null;
        }
    }

    class Linkedlist {
        Node head = null;

        void insert(int data) {
            Node newnode = new Node(data);
            if (head == null) {
                head = newnode;
                return;
            }
            Node curr = head;
            while (curr.next != null) {
                curr = curr.next;
            }
            curr.next = newnode;
        }

        void delete(int n, int k) {
            if (k >= n) {
                head = null;
                return;
            }
            Node curr = head;
            // Move to the (n-k)th node (1-based)
            for (int i = 1; i < n - k && curr != null; i++) {
                curr = curr.next;
            }
            if (curr != null) {
                curr.next = null;  // Cut off the rest of the list
            }
        }

        void print() {
            if (head == null) {
                System.out.print("List is empty");
                return;
            }
            Node curr = head;
            while (curr != null) {
                System.out.print(curr.data + " ");
                curr = curr.next;
            }
        }
    }
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        int n = sc.nextInt();
        Solution s = new Solution();
        Linkedlist ll = s.new Linkedlist();

        for (int i = 1; i <= n; i++) {
            ll.insert(sc.nextInt());
        }
        int k = sc.nextInt();
        ll.delete(n, k);
        ll.print();
    }
}

🟩 Time Complexity - O(N) — [Inserting all nodes ,deleting M nodes requires traversing list.]
🟩 Space Complexity -O(N) — [For storing linked list nodes]
